const layoutMethods = {
  changeBg: function(obj) {
    console.log("on est bien dans le layoutMetyhods -> ", obj.color);
    // obj.el.style.backgroundColor = obj.color;
    // obj.el.classList.add(obj.color);
  }
};

export default layoutMethods;
